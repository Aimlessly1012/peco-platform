# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/.next-build/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# 样板文件例外：它们只含占位值，且 README 的 cp 步骤依赖它们存在于克隆副本里。
# 入库前逐键核查过（2026-09-07）。真值文件仍被上面的 .env* 挡住。
!.env.local.example
!.env.production.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# 环境变量（GitHub OAuth 凭据、数据库连接）
.env.local
.env*.local
