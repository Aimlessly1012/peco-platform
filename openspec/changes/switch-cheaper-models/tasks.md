## 1. 前置验证（无破坏，全部可中止）

- [ ] 1.1 **需用户先充值**——余额为 0 时所有模型一律 402，含标称免费的 `BAAI/bge-m3`，探针无法区分「不可用」与「没钱」
- [ ] 1.2 探针脚本：对每个候选模型发最小请求，记录 HTTP 状态、**实际返回的向量维度**、响应耗时。嵌入候选 `BAAI/bge-m3` / `Pro/BAAI/bge-m3` / `Qwen/Qwen3-Embedding-0.6B`；摘要候选 `Qwen/Qwen3.5-35B-A3B` / `inclusionAI/Ling-mini-2.0` / `stepfun-ai/Step-3.5-Flash`；重排候选 `BAAI/bge-reranker-v2-m3`
- [ ] 1.3 维度实测比对：`BAAI/bge-m3` 返回的维度是否确为 1024，带 `dimensions` 参数与不带是否一致——D2 的「延迟一步失效」就防在这里
- [ ] 1.4 限速探测：对摘要候选做一次并发 4、连发 20 次的压测，记录是否出现 429。触发则改用 `Pro/` 版而非降并发（D5）
- [ ] 1.5 记录调用前后的账户余额差，得到每个候选的真实单价，与定价页比对——定价页对嵌入模型的信息前后矛盾，以实测为准
- [ ] 1.6 依 1.2–1.5 结果定案摘要模型与嵌入模型，写回 design.md 的 Open Questions

## 2. 质量基线（换之前必须有对照）

- [ ] 2.1 在当前模型配置下跑 `services/rag/scripts/eval_retrieval.py`，产出含配置指纹的指标报告
- [ ] 2.2 基线数值与配置指纹落盘到本 change 目录，作为 D4 的「90% 门槛」比较基准
- [ ] 2.3 记一组真实查询的人工抽查结果（ColaMD 上 3–5 条），补足 golden set 基于 mini_repo 的规模局限

## 3. 摘要模型切换（可回退，先做）

- [ ] 3.1 改服务器 `services/rag/.env` 的 `SUMMARY_MODEL`，重建 backend 与 worker（两个都要，同镜像不同容器）
- [ ] 3.2 触发一次 ColaMD 重索引，确认 summarize 阶段走通、报告正常生成
- [ ] 3.3 对比摘要文本质量：新旧模型对同几个文件的摘要产出，判据是「不误导」而非「更精妙」
- [ ] 3.4 记录本次索引的调用次数与余额消耗，与 onyx 那次的单位成本对比

## 4. 嵌入模型迁移（不可逆，严格按序）

- [ ] 4.1 停 worker——避免迁移中途有任务写入旧维度向量（D3 第 2 步）
- [ ] 4.2 DROP 三个 Neo4j 向量索引（索引名以 `graph/client.py` 的 `VECTOR_INDEXES` 为准，不凭记忆写）
- [ ] 4.3 同时改 `EMBEDDING_MODEL` 与 `EMBEDDING_DIM`——只改其一会被启动校验拦下，那是防护生效而非故障
- [ ] 4.4 起 backend，确认三个向量索引按新维度自动重建、启动无维度冲突报错
- [ ] 4.5 起 worker，重索引 ColaMD，确认任务统计里回退原因为嵌入模型变化（验证 `pipeline.py:431` 的漂移检测按预期生效）
- [ ] 4.6 确认 Neo4j 内存占用变化——4096 → 1024 维预期向量存储降约 75%，这是本次的附带收益，要有数

## 5. 验收与决策

- [ ] 5.1 用与 2.1 相同的评测集与配置指纹口径重跑 eval，与基线逐项比对
- [ ] 5.2 指标不低于基线 90% → 保留；低于 → 按 design.md 回滚路径还原嵌入模型（摘要模型的切换独立保留）
- [ ] 5.3 重跑 2.3 的人工抽查查询，确认真实项目上的检索结果没有明显退化
- [ ] 5.4 把新旧模型的单位成本对比（每千文件索引开销）记进 `deploy/server-notes/README.md`

## 6. 收口

- [ ] 6.1 更新 `services/rag/.env.example`：默认值改为定案模型，并修掉「本项目当前不下发 dimensions 参数」这条与 `embedder.py:47` 矛盾的注释
- [ ] 6.2 `deploy/server-notes/README.md` 补两条运维习惯：大仓库索引排在低价时段跑（D6）；换嵌入模型的完整迁移步骤与回滚路径
- [ ] 6.3 确认 `openspec validate --all --strict` 全过，归档本 change
- [ ] 6.4 遗留议题登记（不在本次范围）：OOM 重跑导致摘要重烧、402 重试风暴、`usage` 字段未落日志导致成本无法按阶段归因
